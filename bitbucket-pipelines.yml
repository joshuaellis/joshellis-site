image: node:lts

pipelines:
  custom:
    deploy:
      - step:
          name: Deploy
          deployment: test
          caches:
            - node
          script:
            - apt-get update && apt-get install -y rsync
            - npm install && npm run build
            # - ssh bitbucket@$HOST mkdir -p /var/www/$DOMAIN/$BITBUCKET_REPO_SLUG/
            # - rsync -az --delete ./dist/ bitbucket@$HOST:/var/www/$DOMAIN/$BITBUCKET_REPO_SLUG/
